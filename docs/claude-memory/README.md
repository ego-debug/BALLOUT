# Claude memory snapshot

Claude Code stores its long-term memory *outside* the project folder, so it does
not travel with git. This is a copy, taken 2026-07-18, so the context is present
on every machine.

These are notes-to-self, not documentation — `CLAUDE.md` in the repo root is the
authoritative project context and is what Claude auto-loads.

| File | What it records |
|---|---|
| `ballout-project.md` | What Ballout is, design intent, HQ map facts |
| `research-focus-game-feel.md` | How to research KC: chase exact numbers, not fluff |
| `ballout-git-sync.md` | The GitHub repo + desktop↔laptop workflow |
